# === PDF Processing ===
pymupdf4llm>=0.0.17
PyMuPDF>=1.25.0
paddleocr>=2.9.1
paddlepaddle<3.0.0
pdf2image>=1.17.0
opencv-python>=4.8.0

# === Text Splitting ===
langchain-text-splitters>=0.3.8

# === Embedding ===
# Word2Vec via gensim for local dense embeddings.
gensim>=4.4.0
sentence-transformers>=3.0.0
einops>=0.7.0

# === Vector Database ===
# Butuh dukungan sparse vectors + Query API (Prefetch/Fusion RRF) -> >= 1.10
qdrant-client>=1.12.0

# === HTTP Client (CrossRef API) ===
httpx>=0.28.0

# === API Server & Environment ===
fastapi>=0.115.0
uvicorn>=0.34.0
python-dotenv>=1.0.0
Pillow>=10.0.0

# === Image Captioning (Multimodal RAG) ===
# Qwen2.5-VL berjalan LOKAL (GPU Colab / PC ber-GPU) — tidak ada API key/billing Gemini.
# Qwen2.5-VL butuh transformers >= 4.49; accelerate untuk device_map="auto".
transformers>=4.49.0
accelerate>=0.26.0
torchvision>=0.17.0
